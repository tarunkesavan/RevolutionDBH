version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"
  build:
    commands:
      - echo "Nothing to build"

artifacts:
  files:
    - appspec.yml           # make sure this is included
    - demo/**/*             # include all demo files
